{% extends 'base.html.twig' %}
{% block body %}
<form action={{path('search')}}>
    trier par id
    <input type="text" name="id">
    <input type="submit">chercher>
</form>

<table border="1"> 
<tr>
    <th>id</th>
    <th>username</th>
    <th>publicationdate</th>
    <th>enabled</th>
    <th>author-id</th>
    {#<th>delete</th>
    <th>details</th>
    <th>update</th>#}
</tr>
{% for book in book %}
  <tr>
   <td>{{ book.id }}</td>
   <td>{{ book.title }}</td>
   <td>{{ book.publicationdate|date("d-m-y") }}</td>
    <td>{{ book.enabled }}</td>
    <td>{{ book.author.username }}</td>
   <td><a href={{path('delete_Book',{'id':book.id})}}>Delete</a></td>
   {#<td><a href={{path('detail',{id:book.id})}}>Détails</a></td>#}
   <td><a href={{path('update_book',{id:book.id})}}>Update</a></td>
  </tr>
{% endfor %}
</table>
{% endblock  %}
