{% extends 'base.html.twig' %}
{% block body %}
{% if Authors is empty %}
<p>il n'y a plus de produit</p>
{% else %}
<table border="1"> 
<tr>
    <th>id</th>
    <th>picture</th>
    <th>username</th>
    <th>email</th>
    <th>nb_books</th>
    <th>details</th>
    <th>update</th>
</tr>
    {% set nb_books_tot = 0 %}
    {% for author in Authors %}
        {% set nb_books_tot = nb_books_tot + author.nb_books %}
        <tr>
            <td>{{ author.id }}</td>
            <td><img src="{{asset(author.picture)}}" alt"Taha_Hussein" width='100px' height='100px' /></td>
            <td>{{ author.username | upper }}</td>
            <td>{{ author.email }}</td>
            <td>{{ author.nb_books }}</td>
            <td><a href={{path('detail',{id:author.id})}}>Détails</a></td>
            <td><a href={{path('update_Author',{id:author.id})}}>Update</a></td>
    {% endfor %}
        </tr>
</table>
{% endif %}
<h1>Le nombre d'auteurs est : {{Authors | length}}</h1>
<h1>Le nombre des livres totales est : {{nb_books_tot}}</h1>
    {% endblock %}